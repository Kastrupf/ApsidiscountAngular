import { StockCouleurDirective } from './stock-couleur.directive';

describe('StockCouleurDirective', () => {
  it('should create an instance', () => {
    const directive = new StockCouleurDirective();
    expect(directive).toBeTruthy();
  });
});
